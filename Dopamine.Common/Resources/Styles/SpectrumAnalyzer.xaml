<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:svl="clr-namespace:WPFSoundVisualizationLib;assembly=WPFSoundVisualizationLib">

    <LinearGradientBrush x:Key="SpectrumBarBrush"
                         EndPoint="0.5,0"
                         StartPoint="0.5,1">
        <GradientStop Color="#FF146B"
                      Offset="0" />
        <GradientStop Color="#FF146B"
                      Offset="0.45" />
        <GradientStop Color="#FFB933"
                      Offset="1" />
    </LinearGradientBrush>
    <Style x:Key="SpectrumAnalyzer" TargetType="{x:Type svl:SpectrumAnalyzer}">
        <Setter Property="BarStyle">
            <Setter.Value>
                <Style TargetType="{x:Type Rectangle}">
                    <Setter Property="Fill"
                            Value="{DynamicResource RG_AccentBrush}" />
                    <Setter Property="RadiusX"
                            Value="0" />
                    <Setter Property="RadiusY"
                            Value="0" />
                </Style>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
