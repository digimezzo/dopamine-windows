<baseviews:PlaylistViewBase x:Class="Dopamine.Views.FullPlayer.Collection.CollectionPlaylists"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                            xmlns:dc="clr-namespace:Digimezzo.WPFControls;assembly=Digimezzo.WPFControls"
                            xmlns:pc="clr-namespace:Dopamine.Controls"
                            xmlns:baseviews="clr-namespace:Dopamine.Views.Common.Base"
                            xmlns:commonviews="clr-namespace:Dopamine.Views.Common"
                            xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
                            xmlns:prismMvvm="clr-namespace:Prism.Mvvm;assembly=Prism.Wpf"
                            xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
                            xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                            mc:Ignorable="d"
                            d:DesignHeight="300" d:DesignWidth="300"
                            prismMvvm:ViewModelLocator.AutoWireViewModel="True">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <DockPanel>
        <Border DockPanel.Dock="Right" Width="280" Background="{DynamicResource Brush_Background3}">
            <StackPanel VerticalAlignment="Top" Width="250" Margin="20">
                <commonviews:CoverArtControl x:Name="CoverArt" Width="250" Height="250"/>
                <commonviews:PlaybackInfoControl Height="85" Margin="0,20,0,0" TitleFontSize="22" TitleFontWeight="Light" ShowAlbumInfo="True"/>
            </StackPanel>
        </Border>
        <dc:MultiPanePanel 
            ContentResizeDelay="25" 
            IsRightPaneCollapsed="True" 
            LeftPaneWidthPercent="{Binding LeftPaneWidthPercent, Mode=TwoWay}" 
            LeftPaneMinimumWidth="150"
            MiddlePaneMinimumWidth="250">
            <dc:MultiPanePanel.LeftPaneContent>
                <Border Background="{DynamicResource Brush_Background3}">
                    <DockPanel Margin="20,20,20,26">
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,20">
                            <Button x:Name="PlaylistsButton" ToolTip="{DynamicResource Language_All_Playlists}" Style="{StaticResource TransparentButton}" Click="PlaylistsButton_Click">
                                <dc:LabelToLower Content="{DynamicResource Language_Playlists}" 
                                                 Foreground="{DynamicResource Brush_Foreground1}"
                                                 FontWeight="Light"
                                                 FontSize="20"
                                                 Padding="0"/>
                            </Button>
                            <Label Content="{Binding PlaylistsCount}" Style="{StaticResource CountLabel}"/>
                        </StackPanel>
                        <DockPanel>
                            <DockPanel.Resources>
                                <CollectionViewSource x:Key="ListBoxPlaylistsViewSource" Source="{Binding Playlists}">
                                    <CollectionViewSource.SortDescriptions>
                                        <scm:SortDescription PropertyName="SortName" />
                                    </CollectionViewSource.SortDescriptions>
                                </CollectionViewSource>
                            </DockPanel.Resources>
                            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
                                <pc:IconTextButton Glyph="&#xE710;" GlyphSize="13" Margin="0,0,0,0" HorizontalAlignment="Left" Content="{DynamicResource Language_New_Playlist}" Foreground="{DynamicResource Brush_Accent}" Command="{Binding Path=NewPlaylistCommand}"/>
                                <pc:IconTextButton Glyph="&#xE8E5;" GlyphSize="13" Margin="0,10,0,0" HorizontalAlignment="Left" Content="{DynamicResource Language_Open_Playlist}" Foreground="{DynamicResource Brush_Accent}" Command="{Binding Path=OpenPlaylistCommand}" IsHitTestVisible="{Binding IsIndexing,Converter={StaticResource InvertingBooleanConverter}}"/>
                            </StackPanel>
                            <dc:MultiSelectListBox x:Name="ListBoxPlaylists"
                                 dd:DragDrop.IsDragSource="True" 
                                 dd:DragDrop.IsDropTarget="True" 
                                 dd:DragDrop.DropHandler="{Binding}"
                                 dd:DragDrop.UseDefaultDragAdorner="True"
                                 dd:DragDrop.DefaultDragAdornerOpacity="0.5"
                                 VirtualizingStackPanel.IsVirtualizing="True"
                                 VirtualizingPanel.IsVirtualizingWhenGrouping="True"
                                 VirtualizingStackPanel.VirtualizationMode="Recycling"    
                                 Style="{StaticResource ListBoxPlaylists}"
                                 ItemsSource="{Binding Source={StaticResource ListBoxPlaylistsViewSource}}"
                                 KeyUp="ListBoxPlaylists_KeyUp"
                                 BorderThickness="0"
                                 MouseDoubleClick="ListBoxPlaylists_MouseDoubleClick" 
                                 PreviewKeyDown="ListBoxPlaylists_PreviewKeyDown" 
                                 SelectedItem="{Binding SelectedPlaylist}"
                                 SelectionMode="Single">
                                <dc:MultiSelectListBox.InputBindings>
                                    <KeyBinding Key="F2" Command="{Binding RenameSelectedPlaylistCommand}" />
                                    <KeyBinding Key="Delete" Command="{Binding DeleteSelectedPlaylistCommand}" />
                                </dc:MultiSelectListBox.InputBindings>
                            </dc:MultiSelectListBox>
                        </DockPanel>
                    </DockPanel>
                </Border>
            </dc:MultiPanePanel.LeftPaneContent>
            <dc:MultiPanePanel.MiddlePaneContent>
                <DockPanel Margin="20,20,20,26">
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,20">
                        <dc:LabelToLower Content="{DynamicResource Language_Songs}" 
                                                 Foreground="{DynamicResource Brush_Foreground1}"
                                                 FontWeight="Light"
                                                 FontSize="20"
                                                 Padding="0"/>
                        <Label Content="{Binding TracksCount}" Style="{StaticResource CountLabel}"/>
                    </StackPanel>
                    <DockPanel>
                        <DockPanel DockPanel.Dock="Bottom">
                            <ToggleButton x:Name="ShowPicturesButton" DockPanel.Dock="Right" VerticalAlignment="Center"
                              IsChecked="{Binding ShowTrackArt}"
                              Style="{StaticResource TransparentToggleButton}" Command="{Binding UpdateShowTrackArtCommand}" CommandParameter="{Binding IsChecked, RelativeSource={RelativeSource Self}}">
                                <Grid>
                                    <Border ToolTip="{DynamicResource Language_Hide_Song_Covers}" Visibility="{Binding ShowTrackArt,Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="&#xE896;" Style="{StaticResource Material}" Foreground="{DynamicResource Brush_Foreground2}" FontSize="24"/>
                                    </Border>
                                    <Border ToolTip="{DynamicResource Language_Show_Song_Covers}" Visibility="{Binding ShowTrackArt,Converter={StaticResource InvertingBooleanToVisibilityConverter}}">
                                        <TextBlock Text="&#xE8EF;" Style="{StaticResource Material}" Foreground="{DynamicResource Brush_Foreground2}" FontSize="24"/>
                                    </Border>
                                </Grid>
                            </ToggleButton>
                            <pc:TotalsInformation
                                VerticalAlignment="Center"
                                Foreground="{DynamicResource Brush_Foreground2}" 
                                FontSize="13"
                                TotalDurationInformation="{Binding TotalDurationInformation}"
                                TotalSizeInformation="{Binding TotalSizeInformation}"/>
                        </DockPanel>
                        <dc:MultiSelectListBox x:Name="ListBoxTracks"
                         dd:DragDrop.IsDragSource="True" 
                         dd:DragDrop.IsDropTarget="True" 
                         dd:DragDrop.DropHandler="{Binding}"
                         dd:DragDrop.UseDefaultDragAdorner="True"
                         dd:DragDrop.DefaultDragAdornerOpacity="0.5"
                         VirtualizingStackPanel.IsVirtualizing="True"
                         VirtualizingPanel.IsVirtualizingWhenGrouping="True"
                         VirtualizingStackPanel.VirtualizationMode="Recycling"  
                         MouseDoubleClick="ListBoxTracks_MouseDoubleClick"
                         KeyUp="ListBoxTracks_KeyUp"
                         PreviewKeyDown="ListBoxTracks_PreviewKeyDown"
                         Style="{StaticResource ListBoxPlaylistTracks}"
                         ItemsSource="{Binding TracksCvs.View,IsAsync=True}"
                         BorderThickness="0"
                         SelectionMode="Extended">
                            <dc:MultiSelectListBox.InputBindings>
                                <KeyBinding Key="Delete" Command="{Binding RemoveSelectedTracksCommand}" />
                            </dc:MultiSelectListBox.InputBindings>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding SelectedTracksCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBox}}, Path=SelectedItems}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </dc:MultiSelectListBox>
                    </DockPanel>
                </DockPanel>
            </dc:MultiPanePanel.MiddlePaneContent>
        </dc:MultiPanePanel>
    </DockPanel>
</baseviews:PlaylistViewBase>
