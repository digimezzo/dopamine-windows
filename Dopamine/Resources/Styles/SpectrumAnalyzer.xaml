<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cortrols="clr-namespace:Dopamine.Controls">
    <Style x:Key="SpectrumAnalyzer" TargetType="{x:Type cortrols:SpectrumAnalyzer}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type cortrols:SpectrumAnalyzer}">
                     <Path Name="PART_SpectrumPath" IsHitTestVisible="False" Stroke="{TemplateBinding BarBackground}" StrokeThickness="{TemplateBinding BarWidth}">
                        <Path.CacheMode>
                            <BitmapCache/>
                        </Path.CacheMode>
                    </Path>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
