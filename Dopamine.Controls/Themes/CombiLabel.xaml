<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:my="clr-namespace:Dopamine.Controls">

    <Style x:Key="{x:Type my:CombiLabel}" TargetType="{x:Type my:CombiLabel}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type my:CombiLabel}">
                    <StackPanel Orientation="Horizontal">
                        <Label Margin="0" Padding="0" FontStyle="{TemplateBinding FontStyle}" FontWeight="{TemplateBinding FontWeight}" FontSize="{TemplateBinding FontSize}" Content="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}"/>
                        <Label Margin="1,0,0,0" Padding="0" FontStyle="{TemplateBinding FontStyle2}" FontWeight="{TemplateBinding FontWeight2}" FontSize="{TemplateBinding FontSize2}" Content="{TemplateBinding Content2}" Foreground="{TemplateBinding Foreground2}"/>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
